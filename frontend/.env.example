# URL pública del backend.
# En LOCAL (npm run dev) déjala vacía o apuntando a http://localhost:8080/api
# En NETLIFY (producción) defínela como variable de entorno en Site settings → Build & deploy → Environment.

VITE_API_URL=http://localhost:8080/api
